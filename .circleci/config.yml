version: 2.1

jobs:
  test:
    docker:
      - image: cimg/node:15.10.0
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Build Site
          command: npm run build

workflows:
  version: 2
  build:
    jobs:
      - test