version: 2.1
orbs:
  node: circleci/node@3.0.0
jobs:
  build:
    steps:
      - checkout
      - run: npm run build
workflows:
  version: 2
  build:
    jobs:
      - build
  node-tests:
    jobs:
      - node/test

