version: 2.1
orbs:
  node: circleci/node@3.0.0
jobs:
  build:
    steps:
      - checkout
      - run:
        command: |
          npm run build
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
  node-tests:
    jobs:
      - node/test

